{% extends 'base.jinja' %}
{% block content %}

    <!-- Test comment for home -->
    <h1>Pet Listings</h1>

    <ul>
      {% for pet in pets %}
        <img src="{{ pet.photo_url }}" alt="NO IMG">
        <li><a href="/{{ pet.id }}">{{ pet.name }} {% if available %} <b>is available!</b> {% endif %}</a></li>
      {% endfor %}
    </ul>


    <a href="/add" class="btn btn-primary btn-lg w-100"> {# TODO: Route to the right place? #}
        Add a pet
    </a>


{% endblock %}


