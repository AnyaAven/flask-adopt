{% extends 'base.jinja' %}
{% block content %}

    <!-- Test comment for home -->
    <h1>Users</h1>

    <ul>
      {% for pet in pets %}

        <li><a href="/{{ pet.id }}">{{ pet.name }} {% if %} available!</a></li> {# FIXME: is or not for our if condition #}
      {% endfor %}
    </ul>


    <a href="/add" class="btn btn-primary btn-lg w-100"> {# TODO: Route to the right place? #}
        Add a pet
    </a>


{% endblock %}


